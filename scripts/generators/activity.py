from backend.app.models.models import ActivityRecord


def generate_activity_from_admissions(session, services):
    # placeholder if further processing required; admissions module already inserts daily activity
    return
